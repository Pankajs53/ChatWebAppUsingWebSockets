// learned about query paramter how its useful in creating api

// same routes but different methods->also known as chaining of different crud method on same route
// app.route("chat/:id").get(andjanj).put(sjas).delete(mask);

// how we resolve multiple different promise in a single await promise.all
// const [user,chat]=await Promise.all([
//      chat.findbyid(chat),
//      User.findbyid(user)
// ])

// learned about multer -> how it allow us to access the files send in req data

// fake data creation for better and broad testing -> using faker.js

// leraned about how we can effiecently we can manage validation using express-validator librbary. How its help with specific messages 
